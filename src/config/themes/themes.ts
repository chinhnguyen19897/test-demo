export const customInputTheme = {
    field: {
        input: {
            colors: {
                gray: "bg-transparent border-gray-300 text-gray-800 focus:border-orange-500 focus:ring-orange-500"
            }
        }
    }
}

export const customSelectTheme = {
    field: {
        select: {
            colors: {
                gray: "bg-transparent border-gray-300 text-gray-800 focus:border-orange-500 focus:ring-orange-500"
            }
        }
    }
}

export const customTextareTheme = {
    colors: {
        gray: "bg-gray-50 border-gray-300 text-gray-800 focus:border-orange-500 focus:ring-orange-500",
    }
}

export const customCheckboxTheme = {
    root: {
        color: {
            default: "focus:ring-orange-500 dark:ring-offset-orange-500 dark:focus:ring-orange-500 text-orange-500",
        }
    }
}

export const customRadioTheme = {
    root: {
        base: "h-4 w-4 border border-gray-300 focus:ring-2 focus:ring-orange-500 text-orange-500",
    }
}

export const customThemeDropdown = {
    inlineWrapper: "flex items-center pl-2.5 py-0"
}

export const customModal = {
    base: "flex items-start justify-between rounded-t border-b-0 px-5 pt-5",
    title: "text-xl font-semibold text-gray-800",
    close: {
        base: "ml-auto inline-flex items-center rounded-lg bg-transparent p-1.5 text-sm text-gray-400 hover:bg-gray-200 hover:text-gray-900 hidden",

    }

}
